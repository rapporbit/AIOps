# LLM Wiki Conventions

This directory stores generated diagnosis experience pages at runtime.

- Keep only this conventions file in the public repository.
- Do not commit generated `index.md`, `log.md`, `services/`, `patterns/`, `.write.lock`, or diagnosis-specific pages.
- Runtime Wiki content may contain incident details, environment names, logs, or operator notes and should be treated as local private state.
